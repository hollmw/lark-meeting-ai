# ─────────────────────────────────────────────
# LARK MEETING AI — Python Dependencies
# Install: pip install -r requirements.txt
# ─────────────────────────────────────────────

# ── Web Server ────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9

# ── Lark SDK ─────────────────────────────────
lark-oapi==1.3.0

# ── Audio Capture ─────────────────────────────
# pyaudiowpatch: PyAudio fork with WASAPI loopback support for Windows
# This is what allows capturing system audio (all meeting participants)
pyaudiowpatch==0.2.12.8
numpy==1.26.4

# ── AI — Transcription ────────────────────────
openai-whisper==20231117
# torch must be installed separately with CUDA support BEFORE this file:
#   py -3.11 -m pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121

# ── AI — Speaker Diarization ─────────────────
pyannote.audio==3.1.1

# ── AI — LLM ─────────────────────────────────
ollama==0.2.1                 # local via Ollama (optional)
groq>=0.9.0                   # free API — llama-3.3-70b (recommended)
anthropic>=0.28.0             # Claude API (optional)

# ── Config & Utilities ────────────────────────
pyyaml==6.0.1
python-dotenv==1.0.1
httpx==0.27.0
pydantic==2.7.1
